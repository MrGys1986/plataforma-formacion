@extends('layouts.continuing-education')
@section('content')
<x-portal-page title="Oferta de educación continua" description="Cursos, talleres y diplomados disponibles para participantes externos.">
    <div class="grid gap-4 sm:grid-cols-3"><x-quality-stat label="Actividades" :value="$activities->total()" detail="Oferta registrada"/><x-quality-stat label="Publicadas" :value="$activities->getCollection()->where('status', 'publicado')->count()" detail="En esta página" tone="emerald"/><x-quality-stat label="Con costo" :value="$activities->getCollection()->filter(fn($item) => (float) $item->cost > 0)->count()" detail="En esta página" tone="amber"/></div>
    <div class="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse($activities as $activity)<article class="flex flex-col rounded-xl border border-slate-200 p-5 transition hover:border-blue-200 hover:shadow-sm"><div class="flex items-start justify-between gap-3"><span class="rounded-full bg-blue-50 px-2.5 py-1 text-xs font-semibold text-blue-700">{{ $activity->activityType?->name ?? 'Actividad' }}</span><span class="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-600">{{ ucfirst(str_replace('_',' ',$activity->status)) }}</span></div><h2 class="mt-4 font-semibold text-slate-900">{{ $activity->name }}</h2><p class="mt-2 line-clamp-2 text-sm text-slate-500">{{ $activity->description ?: 'Actividad de formación y actualización profesional.' }}</p><dl class="mt-5 grid grid-cols-2 gap-3 border-t border-slate-100 pt-4 text-sm"><div><dt class="text-xs text-slate-500">Modalidad</dt><dd class="mt-1 font-medium">{{ ucfirst($activity->modality ?? 'Por definir') }}</dd></div><div><dt class="text-xs text-slate-500">Duración</dt><dd class="mt-1 font-medium">{{ $activity->duration_hours ?? 0 }} h</dd></div><div><dt class="text-xs text-slate-500">Inicio</dt><dd class="mt-1 font-medium">{{ $activity->start_date?->format('d/m/Y') ?? 'Por definir' }}</dd></div><div><dt class="text-xs text-slate-500">Inversión</dt><dd class="mt-1 font-medium">${{ number_format((float)$activity->cost, 2) }}</dd></div></dl></article>@empty<div class="rounded-xl border border-dashed border-slate-300 px-6 py-12 text-center text-slate-500 md:col-span-2 xl:col-span-3">No hay actividades externas registradas.</div>@endforelse</div>
    @if($activities->hasPages())<div class="mt-6">{{ $activities->links() }}</div>@endif
</x-portal-page>
@endsection

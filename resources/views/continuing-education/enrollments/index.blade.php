@extends('layouts.continuing-education')
@section('content')
<x-portal-page title="Inscripciones externas" description="Solicitudes y avance de participantes en la oferta de educación continua.">
    <div class="grid gap-4 sm:grid-cols-4"><x-quality-stat label="Inscripciones" :value="$enrollments->total()"/><x-quality-stat label="Solicitadas" :value="$enrollments->getCollection()->where('status','solicitada')->count()" detail="En esta página" tone="amber"/><x-quality-stat label="Aprobadas" :value="$enrollments->getCollection()->where('status','aprobada')->count()" detail="En esta página" tone="emerald"/><x-quality-stat label="Completadas" :value="$enrollments->getCollection()->where('completion_status','completado')->count()" detail="En esta página"/></div>
    <div class="mt-6 overflow-hidden rounded-xl border border-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Participante</th><th class="px-5 py-3">Actividad</th><th class="px-5 py-3">Solicitud</th><th class="px-5 py-3">Pago</th><th class="px-5 py-3">Estado</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($enrollments as $item)<tr><td class="px-5 py-4"><p class="font-semibold">{{ $item->user?->name ?? 'Usuario no disponible' }}</p><p class="text-xs text-slate-500">{{ $item->user?->email }}</p></td><td class="px-5 py-4 text-slate-600">{{ $item->activity?->name ?? 'Sin actividad' }}</td><td class="whitespace-nowrap px-5 py-4 text-slate-600">{{ ($item->requested_at ?? $item->created_at)?->format('d/m/Y') }}</td><td class="px-5 py-4"><span class="text-xs font-semibold text-slate-600">{{ ucfirst(str_replace('_',' ',$item->payment_status)) }}</span></td><td class="px-5 py-4"><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $item->status === 'aprobada' ? 'bg-emerald-100 text-emerald-700' : ($item->status === 'rechazada' ? 'bg-rose-100 text-rose-700' : 'bg-amber-100 text-amber-700') }}">{{ ucfirst($item->status) }}</span></td></tr>@empty<tr><td colspan="5" class="px-6 py-12 text-center text-slate-500">No hay inscripciones registradas.</td></tr>@endforelse</tbody></table></div></div>
    @if($enrollments->hasPages())<div class="mt-6">{{ $enrollments->links() }}</div>@endif
</x-portal-page>
@endsection
